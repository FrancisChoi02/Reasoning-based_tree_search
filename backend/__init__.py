# Input: FastAPI application, tree verification, MCTS search factory
# Output: backend package — re-exports core symbols
# Position: Backend service layer for financial spreading API. If modified,
#   update this header and README_backend.md.
