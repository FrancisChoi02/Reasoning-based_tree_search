# utils/database index

| File | Purpose |
|---|---|
| `db_manager.py` | SQLite database initialization (documents/nodes/node_chunks tables), JSON-to-DB loader with recursive node insertion, and document query helpers (list, get by pk or name). CLI interface for standalone usage. |
| `cosmosdb.py` | Placeholder for Cosmos DB integration. |

If folder contents change, update this index.
